import { Silkscreen, Varela } from 'next/font/google'

export const silkscreen = Silkscreen({
    subsets: ['latin'],
    weight: '400',
})

export const verela = Varela({
    subsets: ['latin'],
    weight: '400',
})